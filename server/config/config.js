const config = {
    app: {
        //TODO dynamic proxy in client package.json
        port: 4000
    },
    database: {
        host: 'localhost',
        user: 'root',
        password: '',
        database: 'todo'
    }
};

module.exports = config;
